[package]
name = "inkstone-infra"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
tantivy = "0.22"
thiserror = "1"

inkstone-core = { path = "../core" }
